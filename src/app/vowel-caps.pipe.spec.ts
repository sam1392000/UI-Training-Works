import { VowelCapsPipe } from './vowel-caps.pipe';

describe('VowelCapsPipe', () => {
  it('create an instance', () => {
    const pipe = new VowelCapsPipe();
    expect(pipe).toBeTruthy();
  });
});
